<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>LeBron James - Live Highlights</title>
<style>
    @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@700&display=swap');

    * { margin:0; padding:0; box-sizing:border-box; font-family:'Arial', sans-serif; }

    body {
        background: radial-gradient(circle at top, #1a1a1a, #000);
        color: #fff;
        overflow-x: hidden;
        position: relative;
    }

    /* Particle background */
    .particle {
        position: absolute;
        border-radius: 50%;
        background: gold;
        opacity: 0.8;
        animation: float 10s linear infinite;
    }

    @keyframes float {
        0% { transform: translateY(0) translateX(0); opacity: 0.8;}
        50% { opacity: 0.5; }
        100% { transform: translateY(-1200px) translateX(100px); opacity: 0;}
    }

    header {
        position: relative;
        text-align: center;
        padding: 100px 20px;
        background: linear-gradient(to right, #552583, #FDB927);
        animation: glow 2s ease-in-out infinite alternate;
    }

    @keyframes glow {
        0% { box-shadow: 0 0 20px #FDB927; }
        100% { box-shadow: 0 0 50px #fff; }
    }

    header h1 {
        font-size: 4rem;
        font-family: 'Orbitron', sans-serif;
        letter-spacing: 5px;
        color: gold;
        text-shadow: 0 0 10px #fff;
    }

    header p {
        font-size: 1.5rem;
        margin-top: 10px;
        font-style: italic;
        color: #fff8;
    }

    .crown {
        width: 100px;
        position: absolute;
        top: -20px;
        left: 50%;
        transform: translateX(-50%);
        animation: crown-bounce 1.5s ease infinite;
    }

    @keyframes crown-bounce {
        0%, 100% { transform: translateX(-50%) translateY(0); }
        50% { transform: translateX(-50%) translateY(-15px); }
    }

    nav {
        display: flex;
        justify-content: center;
        background-color: #000;
        position: sticky;
        top: 0;
        z-index: 10;
    }

    nav a {
        color: #fff;
        text-decoration: none;
        padding: 15px 25px;
        transition: 0.3s;
        font-weight: bold;
    }

    nav a:hover {
        background-color: #552583;
        color: #FDB927;
    }

    section {
        padding: 60px 20px;
        max-width: 1000px;
        margin: auto;
        opacity: 0;
        transform: translateY(50px);
        transition: all 1s ease-out;
    }

    section.visible {
        opacity: 1;
        transform: translateY(0);
    }

    section h2 {
        color: #FDB927;
        margin-bottom: 30px;
        text-align: center;
        font-size: 2.5rem;
        text-shadow: 2px 2px #000;
    }

    .stats {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-around;
    }

    .stat {
        background: rgba(85, 37, 131, 0.7);
        padding: 30px;
        margin: 15px;
        border-radius: 15px;
        flex: 1 1 200px;
        text-align: center;
        cursor: pointer;
        position: relative;
        transition: transform 0.3s, background 0.3s;
    }

    .stat:hover {
        transform: translateY(-10px);
        background: rgba(253, 185, 39, 0.8);
        color: #000;
    }

    .stat h3 {
        font-size: 2.5rem;
        margin-bottom: 10px;
        transition: transform 0.3s;
    }

    .stat p { font-size: 1.2rem; }

    .highlight video {
        width: 100%;
        border-radius: 20px;
        margin: 20px 0;
        opacity: 0;
        transform: translateX(-50px);
        transition: all 1s ease-out;
    }

    .highlight video.visible {
        opacity: 1;
        transform: translateX(0);
        box-shadow: 0 0 40px gold;
    }

    footer {
        text-align: center;
        padding: 30px;
        background-color: #000;
        font-size: 0.9rem;
        color: #888;
    }

    html { scroll-behavior: smooth; }
</style>
</head>
<body>

<!-- Particles -->
<script>
    for(let i=0;i<50;i++){
        let particle = document.createElement('div');
        particle.classList.add('particle');
        particle.style.width = `${Math.random()*6+4}px`;
        particle.style.height = particle.style.width;
        particle.style.left = `${Math.random()*100}vw`;
        particle.style.top = `${Math.random()*100}vh`;
        particle.style.animationDuration = `${Math.random()*10+5}s`;
        document.body.appendChild(particle);
    }
</script>

<header>
    <img src="https://upload.wikimedia.org/wikipedia/commons/0/0e/Crown_icon.png" alt="Crown" class="crown">
    <h1>LeBron James</h1>
    <p>The King of Basketball 👑</p>
</header>

<nav>
    <a href="#career">Career</a>
    <a href="#stats">Stats</a>
    <a href="#highlights">Highlights</a>
</nav>

<section id="career">
    <h2>Career</h2>
    <p>LeBron James, drafted first overall in 2003, has become an icon of basketball greatness.  
    He has won NBA championships with Cleveland, Miami, and Los Angeles Lakers, and is known for his athleticism, vision, and leadership both on and off the court.  
    A true king, inspiring millions worldwide with his dedication and legacy.</p>
</section>

<section id="stats">
    <h2>Career Stats</h2>
    <div class="stats">
        <div class="stat" data-id="points" data-target="38000">
            <h3>0</h3>
            <p>Points Scored</p>
        </div>
        <div class="stat" data-id="seasons" data-target="20">
            <h3>0</h3>
            <p>NBA Seasons</p>
        </div>
        <div class="stat" data-id="championships" data-target="4">
            <h3>0</h3>
            <p>NBA Championships</p>
        </div>
        <div class="stat" data-id="allstar" data-target="19">
            <h3>0</h3>
            <p>All-Star Appearances</p>
        </div>
    </div>
</section>

<section id="highlights" class="highlight">
    <h2>Highlights</h2>
    <!-- Replace URLs with real LeBron highlight clips -->
    <video src="https://www.w3schools.com/html/mov_bbb.mp4" muted></video>
    <video src="https://www.w3schools.com/html/movie.mp4" muted></video>
</section>

<footer>
    <p>© 2025 Glory of LeBron James | Live King Experience 👑</p>
</footer>

<script>
    // IntersectionObserver for sections and videos
    const sections = document.querySelectorAll('section');
    const statsElements = document.querySelectorAll('.stat');
    const highlightVideos = document.querySelectorAll('.highlight video');

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if(entry.isIntersecting){
                entry.target.classList.add('visible');

                // Animate stats
                const statElem = entry.target.querySelector('h3');
                if(statElem){
                    const targetValue = parseInt(entry.target.parentElement.dataset.target || statElem.parentElement.dataset.target);
                    if(!statElem.dataset.animated){
                        let current = 0;
                        let step = Math.ceil(targetValue / 200);
                        const interval = setInterval(()=>{
                            current += step;
                            if(current >= targetValue){
                                current = targetValue;
                                clearInterval(interval);
                            }
                            statElem.textContent = current.toLocaleString();
                            statElem.style.transform='scale(1.3)';
                            setTimeout(()=>statElem.style.transform='scale(1)',100);
                        },20);
                        statElem.dataset.animated = true;
                    }
                }

                // Play video
                const video = entry.target.querySelector('video');
                if(video){ 
                    video.play(); 
                    // Increase points while video plays
                    video.addEventListener('timeupdate', ()=>{
                        const pointsElem = document.querySelector('.stat[data-id="points"] h3');
                        let current = parseInt(pointsElem.textContent.replace(/,/g,''));
                        current += 1; // Add 1 point per video frame update
                        pointsElem.textContent = current.toLocaleString();
                    });
                }
            }
        });
    }, {threshold:0.5});

    sections.forEach(sec=>observer.observe(sec));
    statsElements.forEach(stat=>observer.observe(stat));
    highlightVideos.forEach(video=>observer.observe(video));

    // Auto increment small stats over time
    setInterval(()=>{
        const allStats = [
            {id:'points', increment:5},
            {id:'seasons', increment:0},
            {id:'championships', increment:0},
            {id:'allstar', increment:0}
        ];
        allStats.forEach(stat=>{
            const elem = document.querySelector(`.stat[data-id="${stat.id}"] h3`);
            let current = parseInt(elem.textContent.replace(/,/g,''));
            current += stat.increment;
            elem.textContent = current.toLocaleString();
            elem.style.transform='scale(1.2)';
            setTimeout(()=>elem.style.transform='scale(1)',100);
        });
    }, 2000);
</script>

</body>
</html>
